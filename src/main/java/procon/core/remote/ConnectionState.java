package procon.core.remote;

public enum ConnectionState {
    /*public enum ConnectionState {
        None,
        Connected,      // Connection is connected fine
        Connecting,     // Connection is currently attempting a connection
        Disconnected,   // Connection was disconnected manually
        Error,          // Connection was disconnected by error
    }*/
}
